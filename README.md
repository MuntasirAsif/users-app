# Interview Task

A Flutter application demonstrating user listing, search, pull-to-refresh, skeleton loading, and smooth animations using modern Flutter packages. Built with a clean MVVM architecture and responsive UI.

---

## 📦 Features
- Fetch and display a list of users
- Pull-to-refresh support
- Search users by name or email
- Skeleton loader while fetching data
- User details screen with clean design
- Smooth animations for list items
- Responsive UI using `flutter_screenutil`

---

## ⚙️ Packages Used
- **dio** (`^5.9.0`) – HTTP client for API requests
- **flutter_screenutil** (`^5.9.3`) – Responsive UI scaling
- **google_fonts** (`^6.3.3`) – Custom fonts
- **provider** (`^6.1.5+1`) – State management
- **pull_to_refresh** (`^2.0.0`) – Pull-to-refresh functionality
- **flutter_animate** (`^4.5.2`) – Smooth animations
- **skeletonizer** (`^2.1.2`) – Skeleton loaders for loading states
- **go_router** (`^17.0.1`) – Navigation and routing

---

## 🚀 How to Run

1. **Clone the repository**

```bash
git clone https://github.com/MuntasirAsif/users-app
cd interview_task
flutter pub get
```
```bash
flutter run 
```
